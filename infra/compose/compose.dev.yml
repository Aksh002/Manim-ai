# Development compose extension
# “Phase 1/dev”, rendering can happen locally in-process with background tasks
# Usage: docker compose -f docker-compose.yml -f infra/compose/compose.dev.yml up

services:
  api:
    environment:
      USE_QUEUE: "false"
      RENDER_MODE: "local"
  worker:
    profiles:
      - disabled
